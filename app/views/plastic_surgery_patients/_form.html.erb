<% semantic_form_for(@patient, :html => { :multipart => true }) do |f| %>

  <%= render "#{ApplicationHelper::GENERAL_FORM_TEMPLATES_PATH}/error_header", :object => @patient %>
  
  <%= render "#{PatientHelper::PATIENTS_FORM_TEMPLATES_PATH}/personal_information", :f => f, :action => action %>
  <%= render "#{PatientHelper::PATIENTS_FORM_TEMPLATES_PATH}/patient_history_information", :f => f, :action => action %>
	<%= render "#{PatientHelper::PATIENTS_FORM_TEMPLATES_PATH}/patient_clinical_information", :f => f, :action => action %>
  <%= render "#{PatientHelper::PATIENTS_FORM_TEMPLATES_PATH}/patient_surgery_information", :f => f, :action => action %>
  <%= render "#{PatientHelper::PATIENTS_FORM_TEMPLATES_PATH}/evolution_information", :f => f, :action => action %>
  
	<%= render "#{ApplicationHelper::GENERAL_FORM_TEMPLATES_PATH}/form_footer", :f => f, :action => action %>
  
<% end %>
