<div class="fields">
  <%= f.label :location, "Address #{f.collection_select :address_label_id, AddressLabel.all, :id, :label, :include_blank => true}" %>
  <%= f.text_field :location %>
  
  <%= f.label :complement %>
  <%= f.text_field :complement %>
  
  <%= f.hidden_field :_destroy %>
	<%= link_to_remove_fields "X", f %>
</div>
