<%= semantic_form_for(@address_label) do |f| %>

  <%= render "#{ApplicationHelper::GENERAL_FORM_TEMPLATES_PATH}/error_header", :object => @address_label %>

	<%= f.inputs "Address Label" do %>
  	<%= f.input :label %>
	<% end %>
	
	<%= render "#{ApplicationHelper::GENERAL_FORM_TEMPLATES_PATH}/form_footer", :f => f, :action => action %>

<% end %>
